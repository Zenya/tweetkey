= form_for @tweet do |f|
  = f.label :Category
  = f.collection_select :category, Category.all, :id, :name, :prompt => true
  %br
  = f.label :Name
  %br
  = f.text_field :name
  %br
  = f.label :Description
  %br 
  = f.text_area :description
  %br
  = hidden_field :tweet, :author, :value => current_user.email
  = f.submit("Post")
